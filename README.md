# YMBConfigPipeline

This repo makes use of a YAML file to find out the errors in the code such as syntax or linting errors and acts as a demo like how YMB can use specific YAML to test and fix their code before deployment.